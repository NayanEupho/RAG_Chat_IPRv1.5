"""Admin dashboard backend package.

This package is intentionally isolated from the existing chat API and retrieval
graph. It owns admin ingestion metadata, review state, logs, notifications, and
the local admin job queue.
"""

